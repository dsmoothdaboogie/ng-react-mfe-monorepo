# Example Repo (v2): TurboRepo React MFEs + Web Components (r2wc) + Externalized React Runtime (Import Maps)

This repo adds **Choice A**:
- `apps/platform-runtime` publishes ESM React runtime modules + an import map at `/importmaps/react.importmap.json`
- MFEs externalize React so the shell provides it via import maps
