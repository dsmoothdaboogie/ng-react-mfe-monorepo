# Angular 19 Shell Consumer (v2)

Consumes MFEs that externalize React via import maps.

Internal package:
- `packages/mfe-platform` provides:
  - `loadImportMap(url)`
  - `loadMfeEntryScript(url)`

Config:
- `src/assets/shell-config.json`
